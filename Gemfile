# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem 'sierra_postgres_utilities', github: 'UNC-Libraries/sierra-postgres-utilities', tag: 'v0.2.2'

group :test do
  gem 'rspec', '~> 3.0'
end

